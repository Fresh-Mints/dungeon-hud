{
    "scripts": {
        "api": "cd api && npm run start:dev",
        "client": "cd front && npm start",
        "start": "concurrently \"npm run api\" \"npm run client\" "
    },
    "devDependencies": {
        "concurrently": "^1.0.0"
    }
}